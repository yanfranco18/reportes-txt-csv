<configuration>
    <!-- <property name="LOG_PATH" value="C:/reportes/txt/log" /> -->
    <property name="LOG_PATH_DYNAMIC" value="${path.logs}" />

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH_DYNAMIC}/log_reporte_general.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- <fileNamePattern>${LOG_PATH}/log_reporte_general%d{dd-MM-yyyy}.txt</fileNamePattern> -->
            <fileNamePattern>${LOG_PATH_DYNAMIC}/log_reporte_general%d{dd-MM-yyyy}.txt</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="FILE" />
        <appender-ref ref="STDOUT" />
    </root>
</configuration>